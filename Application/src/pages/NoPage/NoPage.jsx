
/**
 * Landing page for routed urls which don't have a page that exists for it
 * @returns {React.JSX.Element} Text element
 */
function NoPage()
{
    return (
        <>
            Error 404. Page was not found.
        </>
    );
}

export default NoPage;
